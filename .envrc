# direnv, optional. Activates this checkout's venv so `ch`, `python` and
# `pytest` are the ones in it rather than whatever else is on PATH.
if [ -f .venv/bin/activate ]; then
  source .venv/bin/activate
fi
